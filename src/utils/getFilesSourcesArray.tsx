import { TGetFilesSourcesArray } from "./types";

const getFilesSourcesArray: TGetFilesSourcesArray = (source) => {
  return source.keys().map(source) as string[];
};

export default getFilesSourcesArray;
